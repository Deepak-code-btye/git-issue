problem:-  fatal: remote origin already exists.

=> git remote -v
		
		OR

=> git remote rm origin
=> git remote add origin git@github.com:username/myapp.git => (enter your repo)



git reinitialized existing git repository

=> rm -rf .git

